---
const teamRoles = [
  {
    title: 'Middle+ менторы',
    isActive: true
  },
  {
    title: 'Карьерные консультанты',
    isActive: false
  },
  {
    title: 'Ревьюеры кода',
    isActive: false
  },
  {
    title: 'Кураторы групп',
    isActive: false
  },
  {
    title: 'Техподдержка',
    isActive: false
  }
];

const currentRole = {
  title: 'Опытные Middle+ разработчики в роли менторов',
  description: 'Каждый ментор имеет опыт работы от 5 лет в крупных компаниях. Они не просто дают теорию, а делятся реальными практиками и помогают применить их в работе.',
  image: '/images/mentors-support.webp'
};
---

<section class="py-24 bg-slate-50">
  <div class="max-w-7xl mx-auto px-6">
    <h2 class="text-4xl font-bold text-slate-900 mb-4">
      Будете заниматься сами, но не в одиночестве —<br />
      вас поддержит опытная команда специалистов
    </h2>

    <div class="mt-12">
      <div class="flex flex-wrap gap-3">
        {teamRoles.map((role) => (
          <button
            class:list={[
              'px-6 py-3 rounded-xl text-sm font-medium transition-all',
              role.isActive 
                ? 'bg-white shadow-sm text-slate-900' 
                : 'bg-slate-100 text-slate-600 hover:bg-slate-200'
            ]}
          >
            {role.title}
          </button>
        ))}
      </div>

      <div class="mt-12 grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <h3 class="text-2xl font-bold text-slate-900 mb-4">
            {currentRole.title}
          </h3>
          <p class="text-lg text-slate-600">
            {currentRole.description}
          </p>
        </div>
        
        <div class="relative">
          <div class="aspect-[4/3] rounded-2xl overflow-hidden bg-white shadow-lg">
            <img
              src={currentRole.image}
              alt="Команда менторов"
              class="w-full h-full object-cover"
              loading="lazy"
            />
          </div>
          
          <!-- Декоративные элементы -->
          <div class="absolute -bottom-6 -right-6 w-24 h-24 bg-indigo-600/10 rounded-full -z-10"></div>
          <div class="absolute -top-6 -left-6 w-32 h-32 bg-orange-600/10 rounded-full -z-10"></div>
        </div>
      </div>
    </div>

    <div class="mt-16 text-center">
      <a 
        href="#" 
        class="inline-flex items-center bg-slate-900 hover:bg-slate-800 text-white font-medium text-lg rounded-xl px-8 py-4 transition-colors shadow-lg hover:shadow-xl"
      >
        Начать учиться бесплатно
      </a>
      <p class="mt-4 text-slate-500 text-sm">
        Попробуйте бесплатный вводный модуль и познакомьтесь с командой
      </p>
    </div>
  </div>
</section>

<style>
  .aspect-[4/3] {
    aspect-ratio: 4/3;
  }
</style>
