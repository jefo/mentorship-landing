---
import { Icon } from 'astro-icon/components';

const requirements = [
  {
    text: 'Знаете основы React, понимаете принципы компонентного подхода и жизненный цикл компонентов',
    icon: 'ri:reactjs-line'
  },
  {
    text: 'Имеете опыт работы с React от 1 года, а если на других фреймворках — от 2 лет',
    icon: 'ri:time-line'
  },
  {
    text: 'Знакомы с современным JavaScript, работали с REST API и имеете базовые знания о состоянии приложения',
    icon: 'ri:code-s-slash-line'
  }
];
---

<section class="py-24">
  <div class="max-w-7xl mx-auto px-6">
    <h2 class="text-4xl font-bold text-slate-900 mb-16">
      Курс подойдёт, если вы
    </h2>

    <div class="grid gap-8">
      {requirements.map(item => (
        <div class="flex items-start gap-6 bg-white rounded-2xl p-8 shadow-sm">
          <div class="w-12 h-12 rounded-xl bg-green-100 flex items-center justify-center flex-shrink-0">
            <Icon name="ri:checkbox-circle-line" class="w-6 h-6 text-green-600" />
          </div>
          
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 rounded-xl bg-indigo-100 flex items-center justify-center">
              <Icon name={item.icon} class="w-6 h-6 text-indigo-600" />
            </div>
            
            <p class="text-xl text-slate-700">
              {item.text}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
